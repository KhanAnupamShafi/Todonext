export type SideNavItems = {
  title: string;
  icon?: JSX.Element | null;
  path: string;
  fill?: string;
};
export type HeroItems = {
  title: string;
  icon?: JSX.Element | null;
  bpm: string;
  pressure: string;
};
